<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 映射 -->
<mapper namespace="com.ck.dao.UserDao">

    <resultMap id="user" type="User">
        <id column="user_id" property="userId"></id>
        <result column="user_name" property="userName"></result>
        <result column="user_password" property="userPassword"></result>
        <result column="user_age" property="userAge"></result>
        <result column="user_sex" property="userSex"></result>
        <result column="user_birthday" property="userBirthday"></result>
        <result column="user_tel" property="userTel"></result>
        <result column="user_email" property="userEmail"></result>
        <collection property="role" ofType="Role">
            <id property="roleId" column="role_id"></id>
            <result column="role_name" property="roleName"></result>
        </collection>
    </resultMap>

    <select id="queryUserById" resultMap="user" parameterType="int">
        select * from user where user_id = #{userId}
    </select>
    <select id="queryUserAll" resultMap="user">
        select * from user
    </select>
    <select id="queryUserByUserName" resultType="com.ck.entity.User" parameterType="java.lang.String" resultMap="user">
        select * from user where user_name = #{username}
    </select>
    <select id="queryUserAndRoleByUserName" resultType="com.ck.entity.User" parameterType="java.lang.String" resultMap="user">
        SELECT*FROM user u,role r,user_role m where u.user_name = #{userName} and u.user_id = m.user_id and m.role_id = r.role_id
    </select>

</mapper>
